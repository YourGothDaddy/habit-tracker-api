services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: habittracker
      POSTGRES_PASSWORD: habittracker1!
      POSTGRES_DB: habittracker
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: